#执行以下命令安装依赖库：pip3  install -r requirements.txt  
#更新pip3 pip3 install --upgrade --no-cache-dir pip
#查看具体库的版本： pip show httpx

llama-index==0.11.14
# 处理docx文件所需，暂时无用
docx2txt==0.8
python-dotenv==1.0.1
#openai 库暂时没有用到
openai==1.50.2
# 使用ollama的模型
llama-index-llms-ollama==0.3.4
# 使用huggingface中的embedding模型
llama-index-embeddings-huggingface==0.3.1
# 添加 llama-index 的 Chroma 向量存储支持
llama-index-vector-stores-chroma==0.2.1
# 添加 llama-index 的 Milvus 向量存储支持
llama-index-vector-stores-milvus==0.2.7

# ZhipuAI API 客户端
zhipuai==2.1.5.20230904

# 使用socks代理
httpx[socks]==0.27.2

chromadb==0.5.15

pymilvus==2.4.7

flask==3.0.3

transformers==4.42.4

#spacy==3.8.2

